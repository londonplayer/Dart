// ignore_for_file: unused_local_variable

void main() {
  int n = 2;
  int n2 = 2;
  bool r = n <= n2;

  // == (equal to)
  // != (different)
  // > (greater then)
  // < (less then)
  // >= (greater or equal to)
  // <= (less or equal to)
  // && (and) - compares values and returns true if all parameters are true
  // || (or) - compares values and returns true if any parameter is true
}
