// ignore_for_file: unused_local_variable

void main() {
  double nota = 8;
  String mensagem = nota >= 5 ? "Aprovador" : "Reprovado";
  print(mensagem);

  // ? (ternary) - used just like an if/else stament. it will evaluate the first string as true
  // and the second one as false. if the condition is true, display the first string/parameter.
  // if not, display the second one
}
